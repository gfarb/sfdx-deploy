import { flags } from '@salesforce/command';
import { AnyJson } from '@salesforce/ts-types';
import { TemplateCommand } from '../../../utils';
export default class Project extends TemplateCommand {
    static description: string;
    static examples: string[];
    static help: string;
    static longDescription: string;
    protected static flagsConfig: {
        projectname: flags.Discriminated<flags.String>;
        template: flags.Discriminated<flags.String>;
        outputdir: flags.Discriminated<flags.String>;
        namespace: flags.Discriminated<flags.String>;
        defaultpackagedir: flags.Discriminated<flags.String>;
        manifest: flags.Discriminated<flags.Boolean<boolean>>;
        loginurl: flags.Discriminated<flags.String>;
    };
    run(): Promise<AnyJson>;
}
